# Force npm to install optional dependencies
optional=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Disable audit during install for faster builds
audit=false

# Use exact versions to avoid issues
save-exact=true

# Force platform-specific binaries
target_platform=linux
target_arch=x64 